{
  "name": "root",
  "private": true,
  "scripts": {
    "deploy": "cd cloudcannon-snippets && np && npm run deploy"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "magic-memo": "^1.0.3"
  },
  "dependencies": {
    "yargs-parser": "^20.2.1"
  }
}
